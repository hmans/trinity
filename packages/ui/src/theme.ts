import { css } from "@emotion/react"

export const defaultTheme = css({
  font: "max(3vmin, 16px)/1 'Helvetica Neue'",
  fontWeight: "bold",
  button: {
    font: "inherit",
    border: "none",
    padding: "1vmin 2vmin"
  }
})
